<%= form_for @song do |form| %>
	<%= form.label :title, 'Titlul cantecului' %>
	<%= form.text_field :title %>
	<br>
	<%= form.label :artist, 'Numele artistului' %>
	<%= form.text_field :artist %>
	<%= submit_tag 'Create' %>
<% end %>
